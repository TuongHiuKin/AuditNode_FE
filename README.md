
  # Build UI for Audit System

  This is a code bundle for Build UI for Audit System. The original project is available at https://www.figma.com/design/GTRFUCanrqQzZfKvtypsbx/Build-UI-for-Audit-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  